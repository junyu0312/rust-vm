[package]
name = "vm-firmware"
version = "0.1.0"
edition = "2024"

[dependencies]
zerocopy = { workspace = true }

[target.'cfg(target_arch = "x86_64")'.dependencies]
kvm-bindings = { workspace = true }
